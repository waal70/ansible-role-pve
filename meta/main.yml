galaxy_info:
  author: waal70 (Andre)
  description: Role to configure powersave settings (governors) for always-on systems
  company: waal70
  platforms:
    - name: Debian
      versions:
        - bookworm

  license: MIT

  min_ansible_version: "2.1"

  galaxy_tags: [debian,
                powersave,
                governor
  ]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
