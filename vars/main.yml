---
# vars file for ansible-role-proxmox
pve_base_dir: /etc/pve
pve_cluster_conf: "{{ pve_base_dir }}/corosync.conf"

# vault passwords:
# Looking for root_pass? It was moved to the private secrets repository
